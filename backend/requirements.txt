# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0

# RAG stack — PIN CHÍNH XÁC version Colab để tương thích chromadb persistent format
sentence-transformers==5.4.1
chromadb==1.5.9
rank-bm25==0.2.2

# LangGraph + LangChain V1 (match notebook)
langgraph==1.1.9
langchain==1.2.15
langchain-core>=1.0,<2.0
langchain-openai>=1.0,<2.0

# LLM client (OpenAI-compatible, points to Groq)
openai>=1.50,<2.0

# Torch CPU-only — loose để pip pick compatible với sentence-transformers 5.x
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.5,<3.0
